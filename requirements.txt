# Transformers v5 RAG System - Dependencies
# Install order matters - torch must come FIRST!

# Core dependencies (INSTALL FIRST)
torch==2.1.0
numpy==1.24.3

# Transformers and embeddings
transformers==4.40.0
sentence-transformers==2.3.1

# MongoDB
pymongo==4.6.0

# Data processing
pandas==2.0.3

# Utilities
tqdm==4.65.0
python-dotenv==1.0.0
pydantic==2.5.0

# GPU/Performance optimization
bitsandbytes==0.41.2
accelerate==0.24.1

# Optional: Flash Attention (uncomment if you want, but not required)
# flash-attn==2.3.5

# Logging
loguru==0.7.2
